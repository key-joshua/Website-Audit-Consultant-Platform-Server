events {}

http {
    upstream backend {
        server server_replicate_1:3000;
        server server_replicate_2:3000;
        server server_replicate_3:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
